"""
Даны два неупорядоченных набора целых чисел (может быть, с повторениями). Выдать без повторений в порядке возрастания 
все те числа, 
которые встречаются в обоих наборах.
На вход подается 2 числа через пробел: n m
n - кол-во элементов первого множества.
m - кол-во элементов второго множества.
Затем подаются элементы каждого множества через пробел в виде строки. ! Писать input() не надо

На входе:
var1 = '5 4' # количество элементов первого и второго множества
var2 = '1 3 5 7 9' # элементы первого множества через пробел
var3 = '2 3 4 5' # элементы второго множества через пробел

На выходе:
3 5
"""

var1 = '5 4'
var2 = '10 20 30 40 50'
var3 = '10 20 30 40 50'

# Превращение первой строки чисел в множество.
set1 = set(map(int, var2.split()))  
set2 = set(map(int, var3.split()))
common_elements = set1.intersection(set2)
sorted_common_elements = sorted(common_elements)
print(*sorted_common_elements)




# def find_common_elements(set_sizes, set1_str, set2_str):
#     """
#     Функция для нахождения и возвращения общих элементов между двумя множествами чисел.
#     Входные множества представлены строками, числа в которых разделены пробелами.
#     """
    

#     # Преобразование строковых представлений множеств в реальные множества целых чисел,
#     # что убирает повторения и упрощает дальнейшую работу.
#     set1 = set(map(int, set1_str.split()))  # Превращение первой строки чисел в множество.
#     set2 = set(map(int, set2_str.split()))  # То же самое для второй строки чисел.
  
#     # Находим пересечение двух множеств, т.е. общие элементы между ними.
#     common_elements = set1.intersection(set2)  # Определение множества общих чисел.

#     # Сортировка общих элементов по возрастанию для создания порядка.
#     sorted_common_elements = sorted(common_elements)  # Упорядочивание найденных общих чисел.

#     # Преобразование отсортированного списка общих элементов обратно в строку,
#     # где элементы разделены пробелами для корректного вывода.
#     result = ' '.join(map(str, sorted_common_elements))  # Сборка итоговой строки.

#     # Возврат итоговой строки с отсортированными общими элементами.
#     return result

# # Подготовка входных данных для функции и их вывод.

# print(find_common_elements(var1, var2, var3))  # Вывод результатов работы функции.














